const mongoose = require('mongoose');
const { PROJECT_STATUS } = require('../utils/constants');

const projectSchema = new mongoose.Schema(
  {
    projectNumber: {
      type: String,
      unique: true,
      // Not required - will be auto-generated by pre-save hook if not provided
    },
    projectType: {
      type: String,
      enum: ['well', 'mosque', 'other', 'educational_center'],
      default: 'well',
    },
    projectName: {
      type: String,
      required: [true, 'Project name is required'],
      trim: true,
    },
    projectNameAr: {
      type: String,
      trim: true,
    },
    description: {
      type: String,
    },
    descriptionAr: {
      type: String,
    },
    country: {
      type: String,
      required: [true, 'Country is required'],
    },
    region: {
      type: String,
    },
    city: {
      type: String,
    },
    location: {
      latitude: Number,
      longitude: Number,
      address: String,
    },
    status: {
      type: String,
      enum: Object.values(PROJECT_STATUS),
      default: PROJECT_STATUS.PLANNED,
    },
    budget: {
      amount: {
        type: Number,
        min: 0,
      },
      currency: {
        type: String,
        default: 'USD',
      },
    },
    contractor: {
      type: mongoose.Schema.Types.ObjectId,
      ref: 'User',
    },
    projectManager: {
      type: mongoose.Schema.Types.ObjectId,
      ref: 'User',
    },
    client: {
      name: String,
      email: String,
      phone: String,
    },
    startDate: {
      type: Date,
    },
    expectedEndDate: {
      type: Date,
    },
    actualEndDate: {
      type: Date,
    },
    progress: {
      type: Number,
      default: 0,
      min: 0,
      max: 100,
    },
    wellDetails: {
      depth: Number,
      diameter: Number,
      waterQuality: String,
      pumpType: String,
      capacity: Number, // liters per hour
    },
    mosqueDetails: {
      area: Number, // square meters
      capacity: Number, // number of worshippers
      minarets: Number,
      domes: Number,
      prayerHalls: Number,
      ablutionFacilities: Number,
      parkingSpaces: Number,
    },
    educationalCenterDetails: {
      area: Number, // square meters
      totalCapacity: Number, // total number of students
      classrooms: Number,
      laboratories: Number,
      library: Boolean,
      computerLabs: Number,
      playground: Boolean,
      parkingSpaces: Number,
      administrativeOffices: Number,
    },
    otherDetails: {
      type: mongoose.Schema.Types.Mixed, // For flexible JSON structure
    },
    customDetails: {
      type: mongoose.Schema.Types.Mixed, // Custom details for any project type
    },
    beneficiaries: {
      estimatedFamilies: Number,
      estimatedPeople: Number,
    },
    googleDriveFolderId: {
      type: String,
    },
    googleDriveFolderUrl: {
      type: String,
    },
    contract: {
      url: String,
      publicId: String,
      fileName: String,
      fileType: String,
      uploadedAt: Date,
      uploadedBy: {
        type: mongoose.Schema.Types.ObjectId,
        ref: 'User',
      },
    },
    projectReports: [{
      url: String,
      publicId: String,
      fileName: String,
      fileType: String,
      uploadedAt: Date,
      uploadedBy: {
        type: mongoose.Schema.Types.ObjectId,
        ref: 'User',
      },
    }],
    tags: [String],
    priority: {
      type: String,
      enum: ['low', 'medium', 'high', 'urgent'],
      default: 'medium',
    },
    isArchived: {
      type: Boolean,
      default: false,
    },
    createdBy: {
      type: mongoose.Schema.Types.ObjectId,
      ref: 'User',
      required: true,
    },
    notes: String,
    // Review fields
    reviewStatus: {
      type: String,
      enum: ['pending', 'reviewed', 'approved', 'needs_revision'],
      default: 'pending',
    },
    reviewedBy: {
      type: mongoose.Schema.Types.ObjectId,
      ref: 'User',
    },
    reviewedAt: {
      type: Date,
    },
    reviewNotes: {
      type: String,
    },
    // Evaluation fields
    evaluation: {
      overallScore: {
        type: Number,
        min: 0,
        max: 10,
      },
      qualityScore: {
        type: Number,
        min: 0,
        max: 10,
      },
      timelineScore: {
        type: Number,
        min: 0,
        max: 10,
      },
      budgetScore: {
        type: Number,
        min: 0,
        max: 10,
      },
      evaluatedBy: {
        type: mongoose.Schema.Types.ObjectId,
        ref: 'User',
      },
      evaluatedAt: {
        type: Date,
      },
      evaluationNotes: {
        type: String,
      },
    },
    // Client evaluation fields (separate from admin evaluation)
    clientEvaluation: {
      starRating: {
        type: Number,
        min: 1,
        max: 7,
      },
      notes: {
        type: String,
      },
      evaluatedAt: {
        type: Date,
      },
    },
  },
  {
    timestamps: true,
  }
);

// Auto-generate project number
projectSchema.pre('save', async function (next) {
  // Only generate if projectNumber is missing or empty
  if (!this.projectNumber || (typeof this.projectNumber === 'string' && this.projectNumber.trim() === '')) {
    try {
      // Get the latest project number to generate the next sequential number
      const latestProject = await mongoose.model('Project')
        .findOne({ projectNumber: { $regex: /^WP-\d{4}-/ } })
        .sort({ projectNumber: -1 })
        .select('projectNumber');

      let nextNumber = 1;
      if (latestProject && latestProject.projectNumber) {
        const match = latestProject.projectNumber.match(/^WP-\d{4}-(\d+)$/);
        if (match) {
          nextNumber = parseInt(match[1], 10) + 1;
        }
      }

      const year = new Date().getFullYear();
      this.projectNumber = `WP-${year}-${String(nextNumber).padStart(5, '0')}`;
    } catch (error) {
      // Fallback if there's an error
      const count = await mongoose.model('Project').countDocuments();
      const year = new Date().getFullYear();
      this.projectNumber = `WP-${year}-${String(count + 1).padStart(5, '0')}`;
    }
  }
  next();
});

// Indexes for search and filtering
projectSchema.index({ projectNumber: 1 });
projectSchema.index({ status: 1 });
projectSchema.index({ country: 1 });
projectSchema.index({ contractor: 1 });
projectSchema.index({ projectManager: 1 });
projectSchema.index({ createdAt: -1 });

module.exports = mongoose.model('Project', projectSchema);

